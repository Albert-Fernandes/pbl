@model IEnumerable<eGames.Models.Time_Patrocinador>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Patrocinador.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Time.Nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Patrocinador.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Time.Nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Time_PatrocinadorId }) |
            @Html.ActionLink("Details", "Details", new { id=item.Time_PatrocinadorId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Time_PatrocinadorId })
        </td>
    </tr>
}

</table>
