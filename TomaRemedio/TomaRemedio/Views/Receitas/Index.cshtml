@model IEnumerable<TomaRemedio.Receita>

@{
    ViewBag.Title = "Index";
}

<h2>Principal</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Data)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Clinica)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Medico)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Especialidade)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Data)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Clinica)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Medico)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Especialidade)
        </td>
        <td>

            <button class="btn-danger"> @Html.ActionLink("Editar", "Edit", new { id = item.Id }) </button>
            <button class="btn-secondary">@Html.ActionLink("Detalhes", "Details", new { id = item.Id }) </button>
            <button class="btn-warning">@Html.ActionLink("Excluir", "Delete", new { id = item.Id })</button>
        </td>
    </tr>
}

</table>
